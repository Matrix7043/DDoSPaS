server:
  port: 4000

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dpaas
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  redis:
    host: localhost
    port: 6379
    timeout: 60000