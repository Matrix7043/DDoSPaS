server:
  port: 4000

spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/dpaas
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  redis:
    host: redis
    port: 6379
    timeout: 60000